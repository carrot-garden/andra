
task wrapper(type: Wrapper) { gradleVersion = '1.6' }

allprojects { project ->
}

subprojects { project ->

	repositories {
		mavenLocal()
		mavenCentral()
	}

	apply plugin: 'idea'
	apply plugin: 'eclipse'

	buildDir '.build'
	
	defaultTasks 'build'

}
